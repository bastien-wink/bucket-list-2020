{% extends 'base.html.twig' %}

{% block body %}
    <h3>Category list :</h3>

    <ul>
        {% for category in categories %}
            <li>
                <a href="{{ path('deleteCategory', {'id':category.id}) }}">X</a>

                {# Autres possibilités (bonus) : #}
                {#<button onclick="window.location={{ path('deleteCategory', {'id':category.id}) }}">X</button>#}
                {#<form target="{{ path('deleteCategory', {'id':category.id}) }}">#}
                    {#<button type="submit">X</button>#}
                {#</form>#}

                {{ category.name }} ({{ category.ideas.count }}) :

                {% for idea in category.ideas %}
                    {{ idea.title }}.
                {% endfor %}

            </li>
        {% endfor %}
    </ul>

{% endblock %}